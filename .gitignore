# Godot 4 generated files
.godot/
*.translation

# Import metadata (regenerated by Godot)
*.import

# Export presets (may contain signing credentials)
export_presets.cfg

# Exported builds
build/

# macOS
.DS_Store

# Mono/C# (not used here, but common in shared Godot repos)
.mono/
data_*/
mono_crash.*.json

# Sprite generation tool: raw model output and reference photos must never be committed
.venv-sprites/
assets/sprites/player/raw/
*.reference.*
__pycache__/
*.pyc
tools/sprite_gen/reference/turnaround_raw.png
tools/sprite_gen/qa_sheets/

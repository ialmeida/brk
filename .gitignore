# Godot 4 generated files
.godot/
*.translation

# Import metadata (regenerated by Godot)
*.import

# Export presets (may contain signing credentials)
export_presets.cfg

# macOS
.DS_Store

# Mono/C# (not used here, but common in shared Godot repos)
.mono/
data_*/
mono_crash.*.json
